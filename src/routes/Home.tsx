import styled from "styled-components";

const MainWrapper = styled.div`
  width: 100%;
  height: 100%;
  background-color: #cbe0f3;
`;

function Home() {
  return <MainWrapper>d</MainWrapper>;
}

export default Home;
